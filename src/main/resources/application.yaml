spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fleettrack_db
    username: postgres
    password: 12345

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080